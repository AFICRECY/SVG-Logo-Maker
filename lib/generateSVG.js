const Square = require("./square");
const Triangle = require("./triangle");
const Circle = require("./circle");

function generateSVG() {

    // const answers = inquirer.prompt(questions);
    // var userText = "";

    if (answers.text.length < 0 || answers.text.length < 3) {
        console.log("Oops! You entered the wrong character length requirement, please enter up to 3 characters");
    }
    else 
    {
    return (userInput = answers.text);
    }

    // if 

    // where all the stuff goes 
    // var shape = 

    // write a function that considers the user inputs and creates a temerate literal string that is fed to the svg logo maker

}









module.exports = generateSVG;